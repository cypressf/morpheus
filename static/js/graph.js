// Generated by CoffeeScript 1.3.3
(function() {
  var Sleep, User, chartC, chartO, getCookie, gettime, mainUser, position, randint, resizeChart, resizeLines, ticks, updateCurrent, updateOverview;

  randint = function(a, b) {
    return Math.floor(Math.random() * (b - 1) + a);
  };

  User = (function() {

    function User(name) {
      this.name = name;
      this.sleeps = [];
    }

    return User;

  })();

  Sleep = (function() {

    function Sleep(start, end) {
      this.start = start;
      this.end = end;
      this.start = new Date(this.start);
      this.end = new Date(this.end);
    }

    return Sleep;

  })();

  mainUser = new User('Gomez');

  chartO = d3.select("#overview-chart");

  chartC = d3.select("#current-chart");

  updateOverview = function() {
    var h;
    h = $('#overview-chart').height();
    return chartO.selectAll("rect").data(mainUser.sleeps).enter().append("rect").attr("x", function(d, i) {
      return i * 6;
    }).attr("y", function(d, i) {
      return position(d.start) * h;
    }).attr("height", function(d, i) {
      return (position(d.end) - position(d.start)) * h;
    }).attr("width", 5);
  };

  updateCurrent = function() {
    var h, the_ticks, tick_count, tw;
    tick_count = 10;
    the_ticks = ticks(tick_count);
    h = $('#current-chart').height();
    tw = $('#current-chart').width();
    chartC.selectAll("rect").data(mainUser.sleeps.slice(-7)).enter().append("rect").attr("y", function(d, i) {
      return position(d.start) * h;
    }).attr("height", function(d, i) {
      return (position(d.end) - position(d.start)) * h;
    }).attr("width", function(d, i) {
      return 60;
    }).attr("x", function(d, i) {
      return i * 62;
    });
    chartC.selectAll("line").data(the_ticks).enter().append("line").attr("y1", function(d) {
      return position(d) * h;
    }).attr("y2", function(d) {
      return position(d) * h;
    }).attr("x1", 0).attr("x2", tw).style("stroke", "rgba(100,100,100,0.3)");
    return chartC.selectAll(".rule").data(the_ticks).enter().append("text").attr("class", "rule").attr("y", function(d) {
      return position(d) * h;
    }).attr("x", 0).attr("dx", 8).attr("text-anchor", "middle").text(function(d) {
      return d.getHours();
    });
  };

  resizeChart = function(chart, idStr, elementCount, spacing) {
    var h, tw, w;
    if (spacing == null) {
      spacing = 1;
    }
    h = $(idStr).height();
    tw = $(idStr).width();
    w = tw / (elementCount + spacing / 2);
    if (w < 5) {
      w = 5;
    }
    return chart.selectAll("rect").transition().duration(0).attr("height", function(d, i) {
      return (position(d.end) - position(d.start)) * h;
    }).attr("width", function(d, i) {
      return w;
    }).attr("x", function(d, i) {
      return (i * (w + spacing)) + (tw - (w + spacing) * (elementCount + 1));
    }).attr("y", function(d, i) {
      return position(d.start) * h;
    });
  };

  resizeLines = function() {
    var h, tw;
    h = $('#current-chart').height();
    tw = $('#current-chart').width();
    return chartC.selectAll("line").transition(0).duration(0).attr("y1", function(d) {
      return position(d) * h;
    }).attr("y2", function(d) {
      return position(d) * h;
    }).attr("x1", 0).attr("x2", tw);
  };

  position = function(d) {
    return ((d.getHours() + d.getMinutes() / 60 + d.getSeconds() / (60 * 60)) / 24 + 0.25) % 1;
  };

  ticks = function(n) {
    var hours, minutes, seconds, tick, _i, _ref, _ref1;
    ticks = [];
    for (tick = _i = 0, _ref = 1 / n; _i <= 1; tick = _i += _ref) {
      _ref1 = gettime(tick), hours = _ref1[0], minutes = _ref1[1], seconds = _ref1[2];
      ticks.push(new Date(2012, 1, 1, hours, minutes, seconds));
    }
    console.log(ticks);
    return ticks;
  };

  gettime = function(n) {
    var hours, minute_fraction, minutes, second_fraction, seconds;
    n = (n - 0.25) % 1;
    if (n < 0) {
      n += 1;
    }
    hours = n * 24;
    minute_fraction = hours % 1;
    minutes = minute_fraction * 60;
    second_fraction = minute_fraction % 1;
    seconds = second_fraction * 60;
    return [hours, minutes, seconds];
  };

  window.morpheus.getDataForUser((function(response) {
    var newSleep, s, _i, _len;
    for (_i = 0, _len = response.length; _i < _len; _i++) {
      s = response[_i];
      newSleep = new Sleep(s.start, s.end);
      mainUser.sleeps.push(newSleep);
    }
    updateOverview();
    updateCurrent();
    resizeChart(chartO, '#overview-chart', mainUser.sleeps.length);
    resizeChart(chartC, '#current-chart', 7, 10);
    return resizeLines();
  }), 'Gomez');

  $(window).resize(function() {
    resizeChart(chartO, '#overview-chart', mainUser.sleeps.length);
    resizeChart(chartC, '#current-chart', 7, 10);
    return resizeLines();
  });

  getCookie = function(name) {
    var cookie, cookieValue, cookies, _i, _len;
    cookieValue = null;
    if (document.cookie && document.cookie !== '') {
      cookies = document.cookie.split(';');
      for (_i = 0, _len = cookies.length; _i < _len; _i++) {
        cookie = cookies[_i];
        cookie = jQuery.trim(cookie);
        if (cookie.substring(0, name.length + 1) === (name + '=')) {
          cookieValue = decodeURIComponent(cookie.substring(name.length + 1));
          break;
        }
      }
    }
    return cookieValue;
  };

  csrfSafeMethod(method)(function() {
    return /^(GET|HEAD|OPTIONS|TRACE)$/.test(method);
  });

  $.ajaxSetup({
    crossDomain: false,
    beforeSend: function(xhr, settings) {
      if (!csrfSafeMethod(settings.type)) {
        return xhr.setRequestHeader("X-CSRFToken", getCookie('csrftoken'));
      }
    }
  });

  $('#current-chart').mousedown(function(e) {
    var data, end, h, hours, minutes, seconds, start, w, x, y, _ref;
    $('#current-chart').mousemove(function(e) {});
    $('#current-chart').mouseup(function(e) {});
    x = e.pageX - this.offsetLeft;
    y = e.pageY - this.offsetTop;
    h = $("#current-chart").height();
    w = $("#current-chart").width();
    _ref = gettime(y / h), hours = _ref[0], minutes = _ref[1], seconds = _ref[2];
    start = new Date(2012, 9, 16, hours, minutes, seconds).valueOf();
    end = new Date().valueOf();
    data = {
      "username": '{{ user.username }}',
      "start": start,
      "end": end
    };
    $.post("/postdata/", data);
    return false;
  });

}).call(this);
